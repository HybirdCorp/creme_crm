# -*- coding: utf-8 -*-

from . import models

to_register = ((models.Status,    'status'),
               (models.Priority,  'priority'),
               (models.Criticity, 'criticity'),
              )
